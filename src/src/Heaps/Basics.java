package Heaps;

import java.util.PriorityQueue;

public class Basics {
    public static <T, E> void main(String[]args){
        //using Priority Queue to Implement Heap
        PriorityQueue<Integer>minHeap=new PriorityQueue<>();

        PriorityQueue<Integer>maxHeap=new PriorityQueue<>();

        PriorityQueue<Character>minHeapC=new PriorityQueue<>();

        PriorityQueue<Character>maxHeapC=new PriorityQueue<>();

        PriorityQueue<T>minHeapT=new PriorityQueue<T>();
        PriorityQueue<E>maxHeapE=new PriorityQueue<E>();

    }
}
